{-# LANGUAGE ScopedTypeVariables #-}

{- | Contains functions for computing inference using solution extension sets.

Currently incomplete: only contains functions that can operate on quasi-regular valuation algebras.
-}
module LocalComputation.Inference.ExtensionSets (

) where

-- TODO: Move `singleSolutionCompute` and `configSet` here.
-- Sorry about the imports... if this was made generic, there
-- wouldn't be imports for things like M.LabelledMatrix as a
-- generic type for 'solution' would have been defined in
-- 'QuasiRegularValuation'
